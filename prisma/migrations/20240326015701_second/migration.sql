BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[DireccionPersona] ALTER COLUMN [Ciudad] NVARCHAR(1000) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
